# SkillPath — client

React + TypeScript + Vite frontend. See the [root README](../README.md) for setup,
data model, and the project as a whole.

```bash
npm run dev     # served with a proxy to the API at http://localhost:4000
npm run build   # production build to dist/
```
